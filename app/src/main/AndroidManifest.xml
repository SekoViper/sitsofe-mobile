<manifest package="com.sitsofe.scanner"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SitsofeApp"
        android:allowBackup="true"
        android:label="SitsofeApp"
        android:icon="@mipmap/sitsofe_app_icon"
        android:roundIcon="@mipmap/sitsofe_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sitsofe" 
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver
            android:name=".barcode.ScanBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.scanner.scan" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
